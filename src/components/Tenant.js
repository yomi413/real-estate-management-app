import React, { Component } from 'react';

class Tenant extends Component {
  render() {
    return (
      <div>
        This will hold the tenant's basic information.
        Name: 'string'
        Lease: boolean
        Rent: integer
      </div>
    )
  }
}

export default Tenant;
